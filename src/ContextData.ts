import React from "react"


const ContextData = React.createContext({})

export const ContextHeader = React.createContext({})
export const ContextCreateContact = React.createContext({})
export const ContextLogIn = React.createContext({})
export const ContextContactOptions = React.createContext({})

export default ContextData